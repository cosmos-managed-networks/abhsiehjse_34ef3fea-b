# This file is used to create docker images using the heighliner binary.
# see: https://github.com/strangelove-ventures/heighliner

- name: abhsiehjse
  dockerfile: cosmos
  build-target: make install
  binaries:
    - /go/bin/newabhishekndjwf
  build-env:
    - LEDGER_ENABLED=false
    - BUILD_TAGS=muslc